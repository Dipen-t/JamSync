services:
  - type: web
    name: jamsync-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    # Note: PORT is auto-assigned by Render, don't set it manually

